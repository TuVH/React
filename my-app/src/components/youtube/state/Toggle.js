import React,{useState} from 'react';

const Toggle = () => {
    const arr = useState(false);
    const [on, setOn] = arr;
    console.log(on,setOn) 
    return (
        <div>
            
        </div>
    );
};

export default Toggle;